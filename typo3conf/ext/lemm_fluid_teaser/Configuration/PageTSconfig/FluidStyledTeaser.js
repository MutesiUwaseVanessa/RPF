Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, )))))))))))))))))))))))));
Add;
the;
teaser;
to;
the;
"New Content Element Wizard";
Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, Math.pow(, mod.wizards.newContentElement.wizardItems.common)))))))))))))))))))))))));
{
    elements;
    {
        lemm_fluid_teaser;
        {
            iconIdentifier = content - image;
            title = LLL;
            EXT: lemm_fluid_teaser / Resources / Private / Language / locallang_be.xlf;
            wizard.title;
            description = LLL;
            EXT: lemm_fluid_teaser / Resources / Private / Language / locallang_be.xlf;
            wizard.description;
            tt_content_defValues;
            {
                CType = lemm_fluid_teaser;
            }
        }
    }
    show:  = addToList(lemm_fluid_teaser);
}
